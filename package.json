{
  "private": true,
  "license": "Apache-2.0",
  "scripts": {
    "dev": "concurrently -n server,web -c blue,magenta \"DOTENV_CONFIG_PATH=$PWD/.env pnpm --filter @sketch/server dev\" \"pnpm --filter @sketch/web dev\"",
    "build": "pnpm -r build && cp -r packages/web/dist packages/server/dist/public",
    "lint": "biome check .",
    "format": "biome format . --write",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r --workspace-concurrency=1 test",
    "test:unit": "pnpm -r test:unit",
    "test:integration": "pnpm -r --workspace-concurrency=1 test:integration",
    "test:changed": "pnpm -r test:changed",
    "worktree:create": "tsx scripts/worktree.ts create",
    "worktree:remove": "tsx scripts/worktree.ts remove",
    "worktree:list": "tsx scripts/worktree.ts list",
    "prepare": "husky"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.0",
    "concurrently": "^9.2.1",
    "husky": "^9.1.7",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["better-sqlite3", "@biomejs/biome", "esbuild"]
  },
  "dependencies": {
    "@opentelemetry/api": "^1.9.0"
  }
}
